#pragma once

/*
 * run_snake - launch the Snake game.
 *
 * Takes over the screen, enables game-mode keyboard input, and loops
 * forever (press R after game over to restart).
 * Never returns.
 */
void run_snake(void);
